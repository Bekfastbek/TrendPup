{
  "Tools": [
    {
      "type": "function",
      "name": "query_balances",
      "description": "Query the balance of tokens for the current address",
      "strict": false,
      "parameters": {
        "type": "object",
        "properties": {
          "denom_list": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "List of token denominations to query (e.g., ['INJ', 'USDT']). If empty, returns all balances"
          }
        },
        "required": []
      }
    },
    {
      "type": "function",
      "name": "transfer_funds",
      "description": "Transfer funds from current address to another Injective address",
      "strict": false,
      "parameters": {
        "type": "object",
        "properties": {
          "to_address": {
            "type": "string",
            "description": "Recipient's Injective address"
          },
          "amount": {
            "type": "string",
            "description": "Amount to transfer (as Decimal string to preserve precision)"
          },
          "denom": {
            "type": "string",
            "description": "Token denomination (e.g., 'INJ', 'USDT')"
          }
        },
        "required": ["to_address", "amount", "denom"]
      }
    },
    {
      "type": "function",
      "name": "query_spendable_balances",
      "description": "Query the spendable balance of tokens for the current address",
      "strict": false,
      "parameters": {
        "type": "object",
        "properties": {
          "denom_list": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "List of token denominations to query (e.g., ['INJ', 'USDT']). If empty, returns all spendable balances"
          }
        },
        "required": []
      }
    },
    {
      "type": "function",
      "name": "query_total_supply",
      "description": "Query the total supply of tokens on the network",
      "strict": false,
      "parameters": {
        "type": "object",
        "properties": {
          "denom_list": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "List of token denominations to query (e.g., ['INJ', 'USDT']). If empty, returns total supply for all tokens"
          }
        },
        "required": []
      }
    }
  ]
}
```

### Changes Made:
1. Added `"strict": false` property for all functions as per your specification.
2. Reformatted to match the proper structure and indentation for clarity.

Let me know if anything else needs adjustment!

## Me
```json
[
    {
        "type": "function",
        "function": {
            "name": "send_bid_auction",
            "description": "Sends a bid to the burn auction on behalf of the address",
            "parameters": {
                "type": "object",
                "properties": {
                    "round": {
                        "type": "integer",
                        "description": "This specifies the round of the burn auction to participate in"
                    },
                    "amount": {
                        "type": "string",
                        "description": "This specifies the amount to bid"
                    }
                },
                "required": ["round", "amount"]
            }
        }
    },
    {
        "type": "function",
        "function": {
            "name": "fetch_auctions",
            "description": "This functions fetches all the historical burn auctions along with current burn auction",
            "parameters": {
                "type": "object",
                "properties": {},
                "required": []
            }
        }
    },
    {
        "type": "function",
        "function": {
            "name": "fetch_latest_auction",
            "description": "This function fetches the latest burn auction round",
            "parameters": {
                "type": "object",
                "properties": {},
                "required": []
            }
        }
    },
    {
        "type": "function",
        "function": {
            "name": "fetch_auction_bids",
            "description": "Fetches the bids for the particular auction round",
            "parameters": {
                "type": "object",
                "properties": {
                    "bid_round": {
                        "type": "integer",
                        "description": "This specifies the round of the burn auction to participate in"
                    }
                },
                "required": ["bid_round"]
            }
        }
    }
]
```
now this file

## AI Assistant
Here’s the reformatted version of your JSON using a clearer structure, ensuring it's aligned with the conventions:

```json
{
  "Tools": [
    {
      "type": "function",
      "name": "send_bid_auction",
      "description": "Sends a bid to the burn auction on behalf of the address",
      "strict": false,
      "parameters": {
        "type": "object",
        "properties": {
          "round": {
            "type": "integer",
            "description": "This specifies the round of the burn auction to participate in"
          },
          "amount": {
            "type": "string",
            "description": "This specifies the amount to bid"
          }
        },
        "required": ["round", "amount"]
      }
    },
    {
      "type": "function",
      "name": "fetch_auctions",
      "description": "This function fetches all the historical burn auctions along with the current burn auction",
      "strict": false,
      "parameters": {
        "type": "object",
        "properties": {},
        "required": []
      }
    },
    {
      "type": "function",
      "name": "fetch_latest_auction",
      "description": "This function fetches the latest burn auction round",
      "strict": false,
      "parameters": {
        "type": "object",
        "properties": {},
        "required": []
      }
    },
    {
      "type": "function",
      "name": "fetch_auction_bids",
      "description": "Fetches the bids for the particular auction round",
      "strict": false,
      "parameters": {
        "type": "object",
        "properties": {
          "bid_round": {
            "type": "integer",
            "description": "This specifies the round of the burn auction to participate in"
          }
        },
        "required": ["bid_round"]
      }
    }
  ]
}
```

### Changes Made:
1. Eliminated redundant `"function"` objects and de-nested the structure for clarity.
2. Added `"strict": false` to all functions, ensuring uniformity across the structure.
3. Reformatted JSON for better human readability and aligned the properties.
4. Standardized the structure to match the format used earlier (e.g., single `"Tools"` array containing all functions). 

Let me know if additional updates are needed!

## Me
```json
[
    {
        "type": "function",
        "function": {
            "name": "send_bid_auction",
            "description": "Sends a bid to the burn auction on behalf of the address",
            "parameters": {
                "type": "object",
                "properties": {
                    "round": {
                        "type": "integer",
                        "description": "This specifies the round of the burn auction to participate in"
                    },
                    "amount": {
                        "type": "string",
                        "description": "This specifies the amount to bid"
                    }
                },
                "required": ["round", "amount"]
            }
        }
    },
    {
        "type": "function",
        "function": {
            "name": "fetch_auctions",
            "description": "This functions fetches all the historical burn auctions along with current burn auction",
            "parameters": {
                "type": "object",
                "properties": {},
                "required": []
            }
        }
    },
    {
        "type": "function",
        "function": {
            "name": "fetch_latest_auction",
            "description": "This function fetches the latest burn auction round",
            "parameters": {
                "type": "object",
                "properties": {},
                "required": []
            }
        }
    },
    {
        "type": "function",
        "function": {
            "name": "fetch_auction_bids",
            "description": "Fetches the bids for the particular auction round",
            "parameters": {
                "type": "object",
                "properties": {
                    "bid_round": {
                        "type": "integer",
                        "description": "This specifies the round of the burn auction to participate in"
                    }
                },
                "required": ["bid_round"]
            }
        }
    }
]
```
now this

## AI Assistant
Here is the adjusted and reformatted version of your JSON:

```json
{
  "Tools": [
    {
      "type": "function",
      "name": "send_bid_auction",
      "description": "Sends a bid to the burn auction on behalf of the address",
      "strict": false,
      "parameters": {
        "type": "object",
        "properties": {
          "round": {
            "type": "integer",
            "description": "This specifies the round of the burn auction to participate in"
          },
          "amount": {
            "type": "string",
            "description": "This specifies the amount to bid"
          }
        },
        "required": ["round", "amount"]
      }
    },
    {
      "type": "function",
      "name": "fetch_auctions",
      "description": "This function fetches all the historical burn auctions along with the current burn auction",
      "strict": false,
      "parameters": {
        "type": "object",
        "properties": {},
        "required": []
      }
    },
    {
      "type": "function",
      "name": "fetch_latest_auction",
      "description": "This function fetches the latest burn auction round",
      "strict": false,
      "parameters": {
        "type": "object",
        "properties": {},
        "required": []
      }
    },
    {
      "type": "function",
      "name": "fetch_auction_bids",
      "description": "Fetches the bids for the particular auction round",
      "strict": false,
      "parameters": {
        "type": "object",
        "properties": {
          "bid_round": {
            "type": "integer",
            "description": "This specifies the round of the burn auction to participate in"
          }
        },
        "required": ["bid_round"]
      }
    }
  ]
}