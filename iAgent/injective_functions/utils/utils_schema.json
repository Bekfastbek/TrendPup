{
  "Tools": [
    {
      "type": "function",
      "name": "set_network",
      "description": "This will set the current network to use",
      "strict": false,
      "parameters": {
        "type": "object",
        "properties": {
          "network_type": {
            "type": "string",
            "enum": ["testnet", "mainnet"],
            "default": "mainnet",
            "description": "Network to query (testnet or mainnet)"
          }
        },
        "required": [
          "network_type"
        ]
      }
    }
  ]
}